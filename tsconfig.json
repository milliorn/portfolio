{
  "extends": "astro/tsconfigs/strictest",
  "compilerOptions": {
    // (any overrides you need)
  },
  // Tell tsc where your code lives:
  "include": [
    "src/**/*", // all of your .ts/.tsx/.js/.jsx code
    "src/**/*.astro", // Astro component files
    "astro.config.*" // your config file(s)
  ],
  "exclude": ["node_modules", "dist"]
}
